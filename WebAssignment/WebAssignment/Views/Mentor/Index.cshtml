@model IEnumerable<WebAssignment.Models.Mentor>

@{
    ViewBag.Title = "Index";
}
<div class="hero4">
    <h1 class="text-center animated bounceInDown">Find Mentor</h1>
</div>
<h1 class="text-center" style="font-family:'Merriweather', serif; font-size: 62px; font-weight: 700; letter-spacing: 0.5px;">Mentor List</h1>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.subjects)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.latitude)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.longtitude)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.subjects)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.latitude)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.longtitude)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}
</table>*@
<div class="container">
    <div class="row">
        @foreach(var item in Model){
            var image = "../Content/images/" + @item.name + ".jpg";
        <div class="col-sm-4 playful">
            <a href="/Mentor/Details/@Html.DisplayFor(modelItem => item.ID)">
                <figure class="softeffect">
                    <img src="@image" class="img-responsive" alt="Trail Through Woods - Winona" />
                    <figcaption>
                        <h4>@Html.DisplayFor(modelItem => item.name)</h4>
                        <p>@Html.DisplayFor(modelItem => item.subjects)</p>
                    </figcaption>
                </figure>
            </a>
        </div>

        }
    </div>
</div>
